import Enemy from "../classes/Enemy";
import gameState from "../Interfaces/gameState";
import Position from "../Interfaces/Position";

export default (game:gameState, allEnemies:Enemy[]) => {
    const position:Position = { x: -1, y: -1 };
    return position;
};